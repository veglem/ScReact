import {Component} from "./component";
import {createComponent, createElement, createText} from "./vdom";

export const ScReact = {
    createElement,
    createText,
    createComponent,
    Component
}